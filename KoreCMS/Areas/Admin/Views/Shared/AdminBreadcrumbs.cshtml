<ul class="breadcrumb">
    <li><a href="@Url.Action("Index", "Home", new { area = KoreWebConstants.Areas.Admin })" title="Go to Home"><i class="fa fa-home"></i> @T(KoreWebLocalizableStrings.General.Home)</a><span class="divider"></span></li>

    @foreach (var item in WorkContext.Breadcrumbs)
    {
        if (!string.IsNullOrEmpty(item.Url))
        {
            <li><a href="@item.Url"><i class="@item.Icon"></i>@item.Text</a><span class="divider"></span></li>
        }
        else
        {
            <li><i class="@item.Icon"></i>@item.Text<span class="divider"></span></li>
        }
    }
</ul>

