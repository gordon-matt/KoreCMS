@model IEnumerable<Kore.Web.ContentManagement.Areas.Admin.ContentBlocks.IContentBlock>

@{
    Layout = null;
}

@foreach (var block in Model.OrderBy(x => x.Order))
{
    @Html.Partial(block.DisplayTemplatePath, block)
}